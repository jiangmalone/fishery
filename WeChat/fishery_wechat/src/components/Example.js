import React from 'react';
import parseForm from '../utils/parseToForm.js'
const Example = () => {

  return (
    <div>
      YUGUANZAIXIAN
    </div>
  );
};

Example.propTypes = {
};

export default Example;
